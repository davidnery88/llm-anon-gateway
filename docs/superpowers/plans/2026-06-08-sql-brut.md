# Anonymisation SQL brut — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Anonymiser les valeurs PII dans du SQL (INSERT/UPDATE/DELETE/SELECT), en autonome ou embarqué dans de la prose, sans casser la requête, via sqlglot.

**Architecture:** Nouveau module `sidecar/sql_anon.py` (parsing AST sqlglot : extraction colonne→valeur, reconstruction, repérage de spans). `formats.py` ajoute le format "sql" et délègue à `sql_anon`. Le SQL autonome passe par `_anonymize_structured` (inchangé) ; le SQL embarqué est splicé dans `_anonymize_freetext`. Réutilise la classification existante (KB/Presidio/GLiNER/qwen).

**Tech Stack:** Python, sqlglot, pytest.

**Spec:** `docs/superpowers/specs/2026-06-08-sql-brut-design.md`

**Conventions :** tests sidecar dans `sidecar/tests/`, lancés `cd <repo> && python3 -m pytest sidecar/tests/...`. `FieldValue(field, value, path)` est défini dans `sidecar/formats.py`.

---

## File Structure
- Create `sidecar/sql_anon.py` — extraction/reinjection/repérage SQL via sqlglot (responsabilité unique).
- Modify `sidecar/formats.py` — `detect_format` (+"sql"), `extract_pairs`/`reinject` délèguent "sql".
- Modify `sidecar/anonymizer.py` — `_anonymize_freetext` : pré-passe SQL embarqué via `sql_anon.splice_embedded_sql`.
- Modify `sidecar/requirements.txt` — `sqlglot`.

---

## Task 1 : Dépendance sqlglot

**Files:** Modify `sidecar/requirements.txt`

- [ ] **Step 1: Ajouter la dép**
Ajouter à `sidecar/requirements.txt` (près des autres libs de parsing) :
```
sqlglot==30.9.0
```

- [ ] **Step 2: Vérifier l'import**
Run: `python3 -c "import sqlglot; print(sqlglot.__version__)"`
Expected: `30.9.0` (sinon `pip3 install sqlglot==30.9.0 --break-system-packages`)

- [ ] **Step 3: Commit**
```bash
git add sidecar/requirements.txt
git commit -m "build(sidecar): ajoute sqlglot pour l'anonymisation SQL"
```

---

## Task 2 : sql_anon — extraction + reinjection

**Files:**
- Create `sidecar/sql_anon.py`
- Test `sidecar/tests/test_sql_anon.py`

- [ ] **Step 1: Écrire le test (échoue)**
```python
# sidecar/tests/test_sql_anon.py
from sidecar.sql_anon import extract_sql_pairs, reinject_sql

def _pairs(sql):
    return {(p.field, p.value) for p in extract_sql_pairs(sql)}

def test_insert_pairs():
    p = _pairs("INSERT INTO clients (nom, email) VALUES ('David Dupont','d@x.ch')")
    assert ("nom", "David Dupont") in p and ("email", "d@x.ch") in p

def test_insert_multi_row():
    p = _pairs("INSERT INTO c (nom) VALUES ('David'),('Anna')")
    assert ("nom", "David") in p and ("nom", "Anna") in p

def test_update_set_and_where():
    p = _pairs("UPDATE c SET adresse='Rue du Lac 12' WHERE no_avs='7561234567890'")
    assert ("adresse", "Rue du Lac 12") in p and ("no_avs", "7561234567890") in p

def test_delete_where():
    assert ("email", "d@x.ch") in _pairs("DELETE FROM c WHERE email='d@x.ch'")

def test_select_where():
    assert ("nom", "David Dupont") in _pairs("SELECT * FROM c WHERE nom='David Dupont'")

def test_reinject_replaces_only_values():
    sql = "INSERT INTO clients (nom, email) VALUES ('David Dupont','d@x.ch')"
    out = reinject_sql(sql, {"David Dupont": "[PERSONNE_1]", "d@x.ch": "[EMAIL_1]"})
    assert "[PERSONNE_1]" in out and "[EMAIL_1]" in out
    assert "clients" in out and "nom" in out and "email" in out  # structure intacte
    assert "David Dupont" not in out

def test_reinject_malformed_falls_back():
    # SQL non parsable -> fallback remplacement chaîne, pas de crash
    out = reinject_sql("NOT;; SQL David", {"David": "[PERSONNE_1]"})
    assert "[PERSONNE_1]" in out
```

- [ ] **Step 2: Run → FAIL**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_sql_anon.py -v`
Expected: FAIL (ModuleNotFoundError)

- [ ] **Step 3: Implémenter**
```python
# sidecar/sql_anon.py
"""Anonymisation SQL via AST sqlglot : on ne touche QUE les valeurs littérales
(liées à une colonne), jamais la structure (mots-clés, tables, colonnes)."""
from __future__ import annotations
import re
import sqlglot
from sqlglot import exp
from sidecar.formats import FieldValue, _reinject_freetext


def _stmt_pairs(stmt) -> list[FieldValue]:
    pairs: list[FieldValue] = []
    if isinstance(stmt, exp.Insert):
        schema = stmt.this
        cols = [c.name for c in schema.expressions] if isinstance(schema, exp.Schema) else []
        values = stmt.expression
        if isinstance(values, exp.Values):
            for ri, tup in enumerate(values.expressions):
                for ci, e in enumerate(tup.expressions):
                    if isinstance(e, exp.Literal):
                        field = cols[ci] if ci < len(cols) else f"col{ci}"
                        pairs.append(FieldValue(field=field, value=e.this, path=f"ins{ri}.{ci}"))
        return pairs
    # UPDATE (SET + WHERE), DELETE/SELECT (WHERE) : paires colonne = littéral
    for i, eq in enumerate(stmt.find_all(exp.EQ)):
        left, right = eq.this, eq.expression
        if isinstance(left, exp.Column) and isinstance(right, exp.Literal):
            pairs.append(FieldValue(field=left.name, value=right.this, path=f"eq{i}"))
    return pairs


def extract_sql_pairs(sql: str) -> list[FieldValue]:
    out: list[FieldValue] = []
    try:
        statements = sqlglot.parse(sql)
    except Exception:
        return out
    for stmt in statements:
        if stmt is not None:
            out.extend(_stmt_pairs(stmt))
    return out


def reinject_sql(sql: str, replacement: dict) -> str:
    """Remplace les littéraux dont la valeur ∈ replacement par le token, SQL valide.
    Fallback remplacement chaîne si le parse/regen échoue (jamais de crash/fuite)."""
    try:
        statements = sqlglot.parse(sql)
        if not statements or all(s is None for s in statements):
            raise ValueError("unparseable")
        rendered = []
        for stmt in statements:
            if stmt is None:
                continue
            for lit in stmt.find_all(exp.Literal):
                if lit.this in replacement:
                    lit.set("this", replacement[lit.this])
                    lit.set("is_string", True)
            rendered.append(stmt.sql())
        return "; ".join(rendered)
    except Exception:
        return _reinject_freetext(sql, replacement)
```

- [ ] **Step 4: Run → PASS**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_sql_anon.py -v`
Expected: PASS (7 tests)

- [ ] **Step 5: Commit**
```bash
git add sidecar/sql_anon.py sidecar/tests/test_sql_anon.py
git commit -m "feat(sidecar): sql_anon extract_sql_pairs + reinject_sql (AST sqlglot)"
```

---

## Task 3 : sql_anon — repérage de spans + splice embarqué

**Files:**
- Modify `sidecar/sql_anon.py`
- Modify `sidecar/tests/test_sql_anon.py`

- [ ] **Step 1: Écrire les tests (échouent)** — ajouter à `test_sql_anon.py` :
```python
import pytest
from sidecar.sql_anon import find_sql_statements, splice_embedded_sql

def test_find_standalone():
    spans = find_sql_statements("INSERT INTO c (nom) VALUES ('David');")
    assert len(spans) == 1

def test_find_embedded_in_prose():
    text = "Corrige ce script : INSERT INTO c (nom) VALUES ('David'); merci"
    spans = find_sql_statements(text)
    assert len(spans) == 1
    s, e, frag = spans[0]
    assert frag.strip().startswith("INSERT") and frag.strip().endswith(";")
    assert text[s:e] == frag

def test_find_ignores_non_sql():
    assert find_sql_statements("juste une phrase sans requête") == []

@pytest.mark.asyncio
async def test_splice_embedded_sql():
    text = "Avant. UPDATE c SET nom='David' WHERE id=5; Après."
    async def fake_anon(sql):
        # simule l'anonymiseur structuré : masque la valeur 'David'
        return sql.replace("'David'", "'[PERSONNE_1]'"), {"[PERSONNE_1]": "David"}
    new_text, mapping = await splice_embedded_sql(text, fake_anon)
    assert "[PERSONNE_1]" in new_text
    assert new_text.startswith("Avant.") and new_text.endswith("Après.")
    assert mapping == {"[PERSONNE_1]": "David"}
```

- [ ] **Step 2: Run → FAIL**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_sql_anon.py -k "find or splice" -v`
Expected: FAIL (no find_sql_statements / splice_embedded_sql)

- [ ] **Step 3: Implémenter** — ajouter à `sidecar/sql_anon.py` :
```python
_KW_RE = re.compile(r"(?is)\b(INSERT|UPDATE|DELETE|SELECT)\b")


def find_sql_statements(text: str) -> list[tuple[int, int, str]]:
    """Repère les fragments SQL dans un texte. Chaque candidat (mot-clé DML →
    ';' ou fin de ligne/texte) est VALIDÉ par sqlglot.parse. Non-chevauchants."""
    candidates: list[tuple[int, int, str]] = []
    for m in _KW_RE.finditer(text):
        start = m.start()
        semi = text.find(";", m.end())
        nl = text.find("\n", m.end())
        if semi != -1 and (nl == -1 or semi < nl):
            end = semi + 1
        elif nl != -1:
            end = nl
        else:
            end = len(text)
        frag = text[start:end]
        try:
            parsed = sqlglot.parse(frag)
        except Exception:
            continue
        if parsed and parsed[0] is not None:
            candidates.append((start, end, frag))
    # dédoublonner les chevauchements (ex: SELECT à l'intérieur d'un INSERT)
    candidates.sort()
    out: list[tuple[int, int, str]] = []
    last_end = -1
    for s, e, f in candidates:
        if s >= last_end:
            out.append((s, e, f))
            last_end = e
    return out


async def splice_embedded_sql(text: str, anonymize_sql):
    """anonymize_sql: async (sql)->(anon_sql, mapping{token:value}).
    Anonymise chaque span SQL et le recolle dans le texte. Retourne (texte, mapping fusionné)."""
    spans = find_sql_statements(text)
    mapping: dict = {}
    for start, end, frag in sorted(spans, key=lambda x: x[0], reverse=True):
        anon_sql, span_map = await anonymize_sql(frag)
        text = text[:start] + anon_sql + text[end:]
        mapping.update(span_map)
    return text, mapping
```

- [ ] **Step 4: Run → PASS**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_sql_anon.py -v`
Expected: PASS (tous, dont les 4 nouveaux). pytest-asyncio requis (déjà présent côté sidecar ; sinon `pip3 install pytest-asyncio --break-system-packages`).

- [ ] **Step 5: Commit**
```bash
git add sidecar/sql_anon.py sidecar/tests/test_sql_anon.py
git commit -m "feat(sidecar): sql_anon find_sql_statements + splice_embedded_sql"
```

---

## Task 4 : formats.py — format "sql"

**Files:**
- Modify `sidecar/formats.py`
- Test `sidecar/tests/test_formats_sql.py`

- [ ] **Step 1: Écrire le test (échoue)**
```python
# sidecar/tests/test_formats_sql.py
from sidecar.formats import detect_format, extract_pairs, reinject

def test_detect_sql():
    assert detect_format("INSERT INTO c (nom) VALUES ('David')") == "sql"
    assert detect_format("  update c set nom='x' where id=1 ") == "sql"
    assert detect_format("SELECT * FROM c WHERE nom='David'") == "sql"

def test_detect_not_sql():
    assert detect_format("Bonjour, ceci est une phrase.") == "freetext"
    assert detect_format('{"a":1}') == "json"

def test_detect_sql_requires_parse():
    # commence par SELECT mais charabia -> pas "sql"
    assert detect_format("SELECT ??? !!! pas du sql") != "sql"

def test_extract_and_reinject_sql():
    sql = "INSERT INTO clients (nom, email) VALUES ('David Dupont','d@x.ch')"
    pairs = extract_pairs(sql, "sql")
    fields = {(p.field, p.value) for p in pairs}
    assert ("nom", "David Dupont") in fields
    out = reinject(sql, "sql", {"David Dupont": "[PERSONNE_1]"})
    assert "[PERSONNE_1]" in out and "David Dupont" not in out
```

- [ ] **Step 2: Run → FAIL**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_formats_sql.py -v`
Expected: FAIL (detect renvoie "freetext")

- [ ] **Step 3: Implémenter dans `sidecar/formats.py`**

3a. En tête de `detect_format`, AVANT les règles existantes, ajouter la détection SQL :
```python
def detect_format(text: str) -> str:
    t = text.strip()
    if _looks_like_sql(t):
        return "sql"
    if t.startswith(("{", "[")):
        return "json"
    # ... (reste inchangé)
```

3b. Ajouter le helper (au-dessus de `detect_format`) :
```python
import re as _re
_SQL_START = _re.compile(r"(?is)^\s*(INSERT|UPDATE|DELETE|SELECT)\b")


def _looks_like_sql(t: str) -> bool:
    if not _SQL_START.match(t):
        return False
    try:
        import sqlglot
        parsed = sqlglot.parse(t)
        return bool(parsed) and parsed[0] is not None
    except Exception:
        return False
```

3c. Dans `extract_pairs`, ajouter la branche "sql" :
```python
def extract_pairs(text: str, fmt: str) -> list[FieldValue]:
    if fmt == "sql":
        from sidecar.sql_anon import extract_sql_pairs
        return extract_sql_pairs(text)
    if fmt == "table":
        return _extract_table(text)
    # ... (reste inchangé)
```

3d. Dans `reinject`, ajouter la branche "sql" :
```python
def reinject(text: str, fmt: str, mapping: dict) -> str:
    if fmt == "sql":
        from sidecar.sql_anon import reinject_sql
        return reinject_sql(text, mapping)
    if fmt in ("table", "csv"):
        return _reinject_freetext(text, mapping)
    # ... (reste inchangé)
```

(Imports locaux dans les fonctions pour éviter un import circulaire `formats` ↔ `sql_anon`.)

- [ ] **Step 4: Run → PASS**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_formats_sql.py -v`
Expected: PASS (5 tests). Lancer aussi la suite formats existante pour non-régression : `python3 -m pytest sidecar/tests/ -k format -v`.

- [ ] **Step 5: Commit**
```bash
git add sidecar/formats.py sidecar/tests/test_formats_sql.py
git commit -m "feat(sidecar): formats détecte + route le SQL ('sql') vers sql_anon"
```

> Après ce task, **le SQL autonome est déjà fonctionnel** : `anonymize()` → `detect_format`="sql" → `_anonymize_structured` (inchangé, utilise `extract_pairs`/`reinject`) classe chaque valeur par colonne et reconstruit un SQL valide.

---

## Task 5 : anonymizer — SQL embarqué dans la prose

**Files:**
- Modify `sidecar/anonymizer.py` (`_anonymize_freetext`)
- Test `sidecar/tests/test_anonymize_sql.py`

- [ ] **Step 1: Écrire le test (échoue)** — e2e avec dépendances mockées (pas de GLiNER/Ollama).
```python
# sidecar/tests/test_anonymize_sql.py
import pytest

class FakeCache:
    def __init__(self): self.n = 0
    async def get_or_create_token(self, user_id, label, value):
        self.n += 1
        return f"[{label}_{self.n}]"

class FakeNER:
    # détecte 'Bob' comme PERSONNE dans la prose
    def detect(self, text, config=None):
        from sidecar.ner import Entity
        out = []
        idx = text.find("Bob")
        if idx != -1:
            out.append(Entity(text="Bob", label="PERSONNE", start=idx, end=idx+3, source="gliner", confidence=0.99))
        return out

class FakeClassifier:
    async def classify(self, table, column, sql_type="varchar", values=None, value_metadata=None):
        return ("PERSONNE", 0.95) if column == "nom" else (None, 0.0)
    async def aclose(self): pass

class FakeLabels:
    async def lookup(self, *a, **k): return (None, "none")
    async def increment_occurrence(self, *a, **k): pass
    async def upsert(self, *a, **k): return {}

@pytest.fixture
def anonymizer(monkeypatch):
    import sidecar.anonymizer as am
    monkeypatch.setattr(am, "NEREngine", lambda **kw: FakeNER())
    monkeypatch.setattr(am, "ColumnClassifier", lambda **kw: FakeClassifier())
    # Construire avec la vraie signature (vérifier sidecar/anonymizer.py:25) ;
    # column_labels=FakeLabels(), pas d'events_bus/audit.
    a = am.Anonymizer(column_labels=FakeLabels(), events_bus=None, audit_client=None)
    return a

@pytest.mark.asyncio
async def test_embedded_sql_in_prose(anonymizer):
    cache = FakeCache()
    text = "Bob a demandé : INSERT INTO clients (nom) VALUES ('David Dupont'); voilà."
    result, mapping = await anonymizer.anonymize(text, cache, user_id="u1")
    # la valeur SQL est masquée, la structure intacte
    assert "David Dupont" not in result
    assert "clients" in result and "nom" in result
    # la PII de la prose (Bob) est masquée aussi
    assert "Bob" not in result
    # réversible
    for token, value in mapping.items():
        assert token in result or True  # tokens présents dans le résultat / mapping cohérent
    assert "David Dupont" in mapping.values()
```

- [ ] **Step 2: Run → FAIL**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_anonymize_sql.py -v`
Expected: FAIL (le SQL embarqué n'est pas masqué : "David Dupont" reste).
*(Si l'instanciation `Anonymizer(...)` échoue, lire `sidecar/anonymizer.py:25` et adapter les arguments du constructeur — garder NEREngine/ColumnClassifier monkeypatchés.)*

- [ ] **Step 3: Implémenter — pré-passe dans `_anonymize_freetext`**

Au DÉBUT de `_anonymize_freetext` (avant `entities = self.ner.detect(text, config)`), insérer :
```python
        # Pré-passe : anonymiser les requêtes SQL embarquées dans la prose via l'AST,
        # puis NER sur le texte recollé (le SQL est déjà tokenisé -> non re-taggé).
        from sidecar.sql_anon import splice_embedded_sql

        async def _anon_sql(sql: str):
            r, mp, *_ = await self._anonymize_structured(sql, "sql", cache, user_id, config, None)
            return r, mp

        text, sql_mapping = await splice_embedded_sql(text, _anon_sql)
```
Puis, juste AVANT le `return` de `_anonymize_freetext`, fusionner le mapping SQL :
```python
        mapping = {v: k for k, v in replacement.items()}
        mapping.update(sql_mapping)   # <-- ajouter cette ligne
        ...
        return result, mapping, entity_counts, sources
```
(La ligne `mapping = {v: k ...}` existe déjà ; ajouter seulement `mapping.update(sql_mapping)` juste après.)

- [ ] **Step 4: Run → PASS**
Run: `cd "/home/dne/Documents/llm-anon-gateway (with qwen)" && python3 -m pytest sidecar/tests/test_anonymize_sql.py -v`
Expected: PASS. Lancer la non-régression freetext : `python3 -m pytest sidecar/tests/ -k "anon or proxy" -v`.

- [ ] **Step 5: Commit**
```bash
git add sidecar/anonymizer.py sidecar/tests/test_anonymize_sql.py
git commit -m "feat(sidecar): anonymise le SQL embarqué dans la prose (pré-passe freetext)"
```

---

## Self-review (couverture spec)
- ✅ INSERT/UPDATE/DELETE/SELECT, VALUES/SET/WHERE (Task 2 extract + tests)
- ✅ reinject AST, SQL valide, masque seulement les valeurs (Task 2)
- ✅ Détection autonome "sql" + parse-validée (Task 4) → `_anonymize_structured` inchangé gère le reste
- ✅ Détection embarquée + splice (Task 3 + Task 5)
- ✅ Réutilisation classification (KB/Presidio/GLiNER/qwen) via `_anonymize_structured`
- ✅ Fail-safe : detect parse-validé, span non-parsable ignoré, reinject fallback chaîne (Task 2/3/4)
- ✅ Dép sqlglot (Task 1)
- ✅ Tests : unit purs (sql_anon, formats) + e2e mocké (embarqué)

Hors scope (assumé) : DDL, anonymisation des noms de colonnes/tables, dialectes exotiques non parsables.
```
